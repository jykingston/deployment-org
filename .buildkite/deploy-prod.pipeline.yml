steps:
  - block: "Deploy to prod?"
  - label: "🚀 Deploy to prod"
    command: "scripts/deploy prod"
    if: build.branch == 'main'
    concurrency: 1
    concurrency_group: "eft-sure-deploy-prod"
